# Banco de dados (SQLite em dev). Copie para `.env` antes de rodar.
DATABASE_URL="file:./dev.db"

# Segredo para assinatura de sessão (gerar um valor aleatório em produção).
AUTH_SECRET="troque-este-valor-em-producao"
