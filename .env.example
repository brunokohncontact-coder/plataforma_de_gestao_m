# Banco de dados — SQLite em dev (ver DECISIONS.md D3). Em produção, usar Postgres.
DATABASE_URL="file:./dev.db"

# Segredo para assinar sessões (JWT em cookie httpOnly). Gere um valor forte em produção.
AUTH_SECRET="dev-secret-troque-em-producao-com-pelo-menos-32-chars"
