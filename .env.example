# Banco de dados (Prisma). Em dev usamos SQLite (zero dependência externa).
# Para produção, troque o provider para "postgresql" e use uma URL de Postgres.
DATABASE_URL="file:./dev.db"

# Segredo para assinar sessões (cookies). Gere algo aleatório em produção.
AUTH_SECRET="dev-secret-change-me"
