# Banco de dados (SQLite em dev). Em produção, usar postgresql:// e ajustar o provider em prisma/schema.prisma.
DATABASE_URL="file:./dev.db"

# Segredo de sessão/auth (gerar um valor aleatório forte em produção).
AUTH_SECRET="dev-only-insecure-secret-change-me"
