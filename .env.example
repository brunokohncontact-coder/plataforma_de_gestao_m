# Banco de dados (dev: SQLite). Copie para .env: `cp .env.example .env`
DATABASE_URL="file:./dev.db"

# Segredo para assinar a sessão (gere um valor aleatório forte em produção)
AUTH_SECRET="dev-secret-change-me"
