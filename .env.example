# Banco de dados — SQLite em desenvolvimento (zero dependência externa).
# Em produção, troque para uma URL PostgreSQL e ajuste o provider em prisma/schema.prisma.
DATABASE_URL="file:./dev.db"

# Segredo para assinar sessões/cookies. Gere um valor aleatório forte em produção.
AUTH_SECRET="dev-secret-change-me"
