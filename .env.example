# Banco de dados (SQLite em dev — ver DECISIONS.md D3)
DATABASE_URL="file:./dev.db"

# Segredo para assinar a sessão (gere com: openssl rand -hex 32)
AUTH_SECRET="troque-este-segredo-em-producao-com-openssl-rand-hex-32"
